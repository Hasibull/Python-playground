tc = int(input())  # Taking the test case

while tc > 0:
    # taking the value of k and n
    values = input()
    values = values.split()
    k = int(values[0])
    n = int(values[1])

    a = [1] # this will be the final list
    ct = 1

    for i in range(1, k):
        if (i+ct) <= n and ()
